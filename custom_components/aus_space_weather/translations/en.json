"config": {
  "options": {
    "step": {
      "init": {
        "description": "Update your API key and location. Register at {register_url} if needed.",
        "data": {
          "api_key": "API Key",
          "location": "Location"
        }
      }
    }
  }
}
